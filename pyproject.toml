[project]
name = "bitnet-mnist"
version = "0.1.0"
description = "BitNet b1.58 MNIST implementation for testing"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "torchvision>=0.15.0",
]

[tool.uv]
extra-index-url = ["https://download.pytorch.org/whl/cu128"]
